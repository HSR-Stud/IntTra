\begin{tikzpicture}[
    semithick,
    mtext/.style = {inner sep=2mm},
  ]

  \matrix[row sep=1.5cm, column sep=1.5cm] (M) {
    \node[mtext] (time-dgl) {\(\sum_k a_k y^{(k)} = f\)}; &
      \node[mtext] (image-dgl) {\(pY - h = F \)}; \\

    \node[mtext] (time-sol) {\(y\)}; &
      \node[mtext] (image-sol) {\(Y = G \big( F + h \big)\)}; \\
  };

  \draw[->] (time-dgl) to
    node[midway, above] {\(\mathcal{L}\)} (image-dgl);

  \draw[->] (image-dgl) to
    node[midway, right] {\(G = 1/p\)} (image-dgl |- image-sol.north);

  \draw[->] (image-sol) to
    node[pos=.25, above] {\(\mathcal{L}^{-1}\)} (time-sol);

  \draw[->, dashed] (time-dgl) to (time-sol);

  \begin{pgfonlayer}{background}
    \node (T1) at ($(time-dgl.north west) + (-.5, .5)$) {};
    \node (T2) at ($(time-dgl.east |- time-sol.south east) + (.5, -.5)$) {};

    \node (I1) at ($(image-dgl.north west) + (-.5, 0)$) {};
    \node (I2) at ($(image-sol.south east) + (.5, 0)$) {};

    % adjust heights of I to match T
    \node (I1) at (T1 -| I1) {};
    \node (I2) at (T2 -| I2) {};

    \node[color=blue!70!black, above right] at (T1) {Zeitbereich};
    \node[color=magenta!70!black, above right] at (I1) {Bildbereich};

    \fill[color=blue!20] (T1) rectangle (T2);
    \fill[color=magenta!20] (I1) rectangle (I2);
  \end{pgfonlayer}
\end{tikzpicture}
